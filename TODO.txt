TODO:

- implement clearDataset();
- test with multi calls to queryDataset();
- test with mutiple instances of dataset.

- make appropriate message when there is no internet
- make appropriate loading waiting cursor.


- write jToxDataset API:
  - options: editColumns: true / dalse, columnSets: [], 
  - queryDataset(datasetUri);
  - queryFeatures(dataetUri);, group based on sameAs
  - queryEntries(from, size);
  - nextPage();
  - prevPage();


- make toxtree part of jToxKit
