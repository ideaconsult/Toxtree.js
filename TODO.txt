TODO:

- No borders on detailed tab-list
- nicer tabs content
- column show/hide hides the details - do it my way?
- turn south arrow up when openned
- calculate details tablist div
- add filter box
- add pagination controls
- add sorting handlers
- make dataset filtering and sorting
- make manual site requests & table(s) filling
- make appropriate message when there is no internet
- 

- InChi - може да се показва само InChi_Key - като може да се копира цели
- Make empty tabs "disabled";
- Invisible details column right next to the diagram - expanding to the right (animated);
- onDraw interception for sorting

- Detailed information row:
-- Click handler on first column (instead of number)
-- Filling the '#jtox-ds-details' row
-- Making tabs in the '.tablist' div
-- return null on feature-item creating routine ability - for skipping diagram

- table sorting problems;
-- keep the two tables in sync?
-- filtering - separate, making calls to the tables
-- pagination - again, in sync?
- make toxtree part of jToxKit
- 


jToxDataset:
- options: editColumns: true / dalse, columnSets: [], 
- queryDataset(datasetUri);
- queryFeatures(dataetUri);, group based on sameAs
- 

